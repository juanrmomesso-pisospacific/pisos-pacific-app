# Dependencies
node_modules/

# Build output
dist/
build/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# Live database state — seeded fresh from data/*.seed.json on first boot
data/db.json
data/db.json.bak

# Dev artefacts captured during the initial scaffold
*.png
# ...but keep the committed brand logos + the PDF engine logo
!assets/branding/*.png
!pdf/*.png
!pdf/assets/*.png
.playwright-mcp/

# Python (PDF engine)
__pycache__/
*.pyc

# Local env
.env
.env.local

# Source spreadsheets (large; the generated *.seed.json is the committed source of truth)
data/sources/

# tooling de skills (npx skills add)
.agents/
skills-lock.json

# Backups locales de db.json
data/db.json.bak-*
