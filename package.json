{
  "name": "pisos-pacific-local",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd dashboard-app && npm install && npm run build",
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cookie-parser": "^1.4.7",
    "express": "^4.19.2",
    "pdfkit": "^0.19.0"
  }
}
